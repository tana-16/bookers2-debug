<div class="m-1">
  <%= form_with(model:[book, comment], method: :book) do |f| %>
    <div class="form-group">
      <%= f.text_area :comment_content, size: "80x5" %>
    </div>
    <div class="form-group">
      <%= f.submit '送信' %>
    </div>
  <% end %>
</div>